# ActiveCAMT v2.10.0 🎉

# อัปเดต ActiveCAMT ประจำวันที่ 19 ก.ย. 69

สรุปสำหรับวันที่ 19 ก.ย. 69 เอาไว้ลง Discord และให้ทีมที่เกี่ยวข้องอ่านกัน

---

## ฝั่งนักศึกษา (สิ่งที่ user จะเห็น)

- **รับของรางวัลแล้วง่ายขึ้น ไม่ต้องบอกชื่อ/รหัสนักศึกษาเอง** ตอนไปรับของรางวัลไม่ว่าจะเป็นรางวัลในงานหรือของแจกที่ต้องมารับทีหลัง (เช่น แก้ว) เจ้าหน้าที่จะสแกน QR โค้ดของนักศึกษาแทนการจดชื่อ ทำให้เร็วขึ้นและไม่พลาดเรื่องข้อมูลผิด และอาจมีการถ่ายรูปตอนรับของไว้เป็นหลักฐานด้วย

---

## ฝั่งแอดมิน/ทีมงาน (สิ่งที่ staff จะเห็น)

- **เมนูใหม่ "การรับรางวัล" (`/admin/prizes`) สำหรับบันทึกการแจกของรางวัลทุกชนิด** ไม่ว่าจะเป็นรางวัลที่แจกในงานกิจกรรมหรือของแจกที่ให้มารับนอกรอบ (เช่น แก้ว) ก็บันทึกในที่เดียวกันได้ ตั้งค่าได้ว่ารางวัลนี้รับได้คนละครั้งเดียวไหม และต้องเช็คอินกิจกรรมไหนก่อนถึงมีสิทธิ์รับ โดยระบบจะกันไม่ให้คนเดิมรับซ้ำในระดับฐานข้อมูลเลย ต่อให้พยายามรับสองรอบจากเครื่องคนละเครื่องพร้อมกันก็ยังกันได้
- **แจกของผ่านหน้าจอสแกน QR เหมือนเครื่องสแกนเช็คอิน** เลือกรางวัลแล้วสแกน QR นักศึกษา ระบบจะขึ้นชื่อ-รหัสให้ทันที กดยืนยันแล้วค่อยถ่ายรูปนักศึกษาถือของทีหลังได้ ไม่ต้องรอรูปเสร็จก่อนถึงจะบันทึกได้ กันเน็ตล่มแล้วพลาดบันทึกที่บูธ ถ้าใครสแกนไม่ได้ก็ค้นหาด้วยชื่อ/รหัสแทนได้ **SMO ก็แจกรางวัลผ่านหน้านี้ได้เหมือนเดิมที่แจกในเครื่องสแกน** แต่การออกรายงานและตั้งค่ารางวัลใหม่ยังต้องเป็นแอดมิน/องค์กร/ประธานชมรม-สาขาเท่านั้น
- **ออกรายงานส่งคณบดีได้ 2 แบบ** ไฟล์ Excel ที่แก้ไขได้พร้อมรูปถ่ายฝังอยู่ในไฟล์ กับหน้าเว็บสำหรับสั่งพิมพ์เป็น PDF ที่รูปจะติดอยู่กับชื่อคนแน่นอนไม่มีทางสลับ (ต่างจากไฟล์ Excel ที่ถ้ามีคนลอง sort/filter ตาราง รูปที่ฝังไว้จะไม่เลื่อนตามแถวไปด้วย จึงต้องใช้ PDF เป็นตัวหลักฐานจริง)

---

## ฝั่งทีม (technical changelog)

### เพิ่มระบบการรับรางวัล (การรับรางวัล) พร้อมกันข้อมูลซ้ำระดับฐานข้อมูล (PR #237, merge commit `58681f3`)

- **Feature:** เพิ่มตาราง `prizes` และ `prize_claims` โดย **รางวัลไม่ได้ผูกเป็นลูกของอีเวนต์** แต่ผูกกับอีเวนต์แบบ optional เท่านั้น (`event_id` เป็น `SET NULL`) เพราะของแจกอย่างแก้วอาจแจกทั้งในงานและที่เคาน์เตอร์วันหลัง ถ้าผูกความไม่ซ้ำไว้กับอีเวนต์จะทำให้เปิดรอบรับรอบสองแล้วรับซ้ำได้ การกันซ้ำจึงผูกไว้กับตัวรางวัลเองผ่าน **partial unique index** `(prize_id, student_id) WHERE one_per_student` ที่ระดับ Postgres ซึ่งกันได้แม้สแกนพร้อมกันสองเครื่องในวินาทีเดียวกัน (ทดสอบ race นี้จริงแล้วผ่าน)
- **เงื่อนไขเช็คอินก่อนรับ:** ตั้งค่าได้ต่อรางวัลว่าต้องเช็คอินกิจกรรมไหนก่อนถึงมีสิทธิ์รับ (`requireCheckIn` + `eligibilityEventId` แยกจาก `event_id` ที่ใช้บอกแค่บริบทของรางวัล) และปรับ query รายงานให้ pre-aggregate จำนวนวันที่เช็คอินก่อน join เพราะกิจกรรมหลายวันมี attendance หลายแถวต่อคน ถ้า join ตรง ๆ ผู้ชนะคนเดียวจะโผล่ซ้ำในรายงานหลายบรรทัด
- **PDPA:** รูปหลักฐานการรับของเก็บใน private bucket เดียวกับรูปหลักฐาน evidence check-in (ไม่ใช่ public URL) เสิร์ฟผ่าน route ที่ auth แล้วเท่านั้น และทุกครั้งที่มีคนอื่น (ไม่ใช่เจ้าของรูป) เปิดดูหรือดาวน์โหลดรายงานจะถูกบันทึกลง audit log เก็บรูปไว้ไม่มีกำหนดจนกว่า super_admin จะลบ ลบได้เฉพาะรูป แถวบันทึกการรับยังอยู่เสมอ ไม่งั้นระบบกันซ้ำจะลืมว่าคนนี้เคยรับไปแล้ว
- **สิทธิ์เข้าถึง:** แยกสิทธิ์ "แจกรางวัล" (`canAwardPrizes` รวม smo ด้วยเพราะแจกทีละคนต่อหน้า) ออกจาก "ตั้งค่ารางวัล/ออกรายงาน" (`canManagePrizes`/`canExportPrizeReport` ไม่รวม smo เพราะรายงานคือรายชื่อ+รหัส+รูปหน้าทั้งงานในไฟล์เดียว) ประธานชมรม/สาขาถูก scope ให้เห็นเฉพาะรางวัลที่ผูกกับอีเวนต์ของชมรม/สาขาตัวเองผ่าน `EventScopeService` เดียวกับที่ใช้ในหน้า attendance/appeals ทดสอบแล้วว่าประธานคนละชมรมมองไม่เห็นรางวัลของกันและกัน
- **รายงาน:** ไฟล์ `.xlsx` ใช้ `exceljs` (ไม่ใช่ `xlsx` ที่ export อื่นใช้) เพราะ SheetJS community build ที่ pin ไว้ฝังรูปในไฟล์ไม่ได้ ย่อรูปด้วย `sharp` เหลือด้านยาว 320px ก่อนฝัง กันไฟล์ใหญ่เกินไป และเขียนวันที่เป็น real Excel date cell (ไม่ใช่ string) เพื่อให้ sort ได้ถูกต้อง หน้ารายงาน PDF ใช้ query เดียวกันกับ Excel เป๊ะ ๆ ผ่านการสั่งพิมพ์จากเบราว์เซอร์ ไม่ต้องเพิ่ม PDF library
- **Migration:** เพิ่ม step 94 ใน `src/db/migrate.ts` (ตัวสคริปต์ที่ `db:migrate`/`db:migrate:container` รันจริงกับ Postgres) เพราะ `db:generate` สร้างไฟล์ `drizzle/0042` ให้เฉยๆ ไม่ได้ทำให้ตารางไปสร้างบน Postgres จริงโดยอัตโนมัติ ถ้าไม่เพิ่ม step นี้ทุก route ของฟีเจอร์นี้จะ error บน prod ทันทีหลัง deploy ทั้งที่ build ผ่าน (เจอระหว่างรัน `/verify` กับ local DB ก่อน merge)
- **แก้บั๊กที่เจอตอนทดสอบจริง:** route ที่รับ id ของรางวัลจาก URL (`claim`, แก้ไขรางวัล, export, หน้ารายงาน) ไม่ได้ตรวจรูปแบบ id ก่อน query ทำให้ id ที่หายไปหรือผิดรูปแบบหลุดไปถึง Postgres ตรง ๆ จน error ดิบโผล่ใน log แทนที่จะตอบ 400 สั้น ๆ แก้แล้วทุก route ให้ตรวจก่อนเหมือน route รูปหลักฐานที่มีการตรวจอยู่แล้ว
- ตรวจสอบแล้วด้วย `npm run lint`, `npx tsc --noEmit`, `npm run build`, `npm test` (289 tests) ผ่านครบ พร้อมทดสอบจริงกับ local DB ผ่าน dev-bypass session (ยิง API ตรง ไม่ mock): กันรับซ้ำทั้งจาก SQL ตรงและผ่าน API, เงื่อนไขเช็คอินก่อนรับ, การจัดกลุ่มไม่ให้รายงานซ้ำในกิจกรรมหลายวัน, เนื้อหาไฟล์ Excel จริง (วันที่/รูปที่ฝัง), ลบรูปแล้วแถวยังกันซ้ำได้ต่อ, ลบรางวัลแล้ว claims/รูปถูกลบตามจริง

### แก้ log การส่ง push notification ที่ล้มเหลวไม่มีร่องรอย (PR #236, merge commit `8a9fe56`)

- **Bug:** ตอนก่อนหน้านี้ ถ้าส่ง push แล้วล้มเหลว (เช่น VAPID key ผิด, VAPID_SUBJECT ผิดรูปแบบ, หรือ push service ปฏิเสธ) โค้ดจะแค่อัปเดต `failureCount`/ลบ subscription ทิ้งเงียบ ๆ โดยไม่ log อะไรเลย ทำให้เวลาการส่งจริงมีปัญหาบน prod ไม่มีทางรู้สาเหตุได้เลยถ้าไม่เข้าไปดู DB ตรง ๆ
- **แก้ไข:** log รหัส subscription, status code, และข้อความ error จาก push service เอง (ไม่ log endpoint URL เต็มเพราะเป็นข้อมูลระบุตัวอุปกรณ์)

---

อัปเดตรอบนี้รวม PR #237 (ระบบการรับรางวัลพร้อมกันข้อมูลซ้ำระดับฐานข้อมูลและรายงานส่งคณบดี branch `feat/prize-claim` merge commit `58681f3`) และ PR #236 (แก้ log การส่ง push notification ที่ล้มเหลว branch `fix/push-send-error-logging` merge commit `8a9fe56`) ตัดแพ็กเกจเป็น **tag/release v2.10.0** สรุปวันที่ 19 ก.ย. 69

---

# ActiveCAMT v2.10.1 🎉

## ฝั่งแอดมิน/ทีมงาน (สิ่งที่ staff จะเห็น)

- **แก้หน้าจอ "การรับรางวัล" ที่อ่านไม่ออกบนมือถือที่ตั้งเป็นโหมดมืด** ถ้ามือถือของเจ้าหน้าที่ตั้งธีมเป็นมืดไว้ หน้าต่างสร้างรางวัลและหน้าสแกนแจกรางวัลที่เพิ่งเปิดใช้เมื่อกี้จะขึ้นพื้นหลังสีเข้มแต่ตัวหนังสือยังเป็นสีเดิม ทำให้อ่านไม่ออกหรือดูเหมือนตัวหนังสือซ้อนกัน ตอนนี้แก้ให้ใช้สีเดียวกับหน้าแอดมินอื่น ๆ ทั้งระบบ อ่านออกชัดเจนไม่ว่ามือถือจะตั้งธีมแบบไหน และปรับให้ช่องตั้งค่าต่าง ๆ กดง่ายขึ้นด้วย เพราะหน้านี้ใช้งานบนมือถือหน้าบูธเป็นหลัก

## ฝั่งทีม (technical changelog)

- **Bug:** `PrizesClient.tsx` และ `PrizeAwardPanel.tsx` เป็นไฟล์เดียวในทั้ง `src/app/admin` ที่ใช้ Tailwind `dark:` classes ทั้งที่แอปนี้ตั้งใจให้เป็น light-only เท่านั้น (`color-scheme: only light` ใน `globals.css`) เพราะถ้าปล่อยให้ dark mode ทำงาน QR โค้ดเช็คอินสีดำบนขาวจะถูกกลับสีจนเครื่องสแกนอ่านไม่ออก พอมือถือเจ้าหน้าที่ตั้งโหมดมืดไว้ `dark:` classes เหล่านี้เลยทำงานแต่ตัวหนังสือไม่มีสีสำหรับโหมดมืดคู่กัน ทำให้อ่านไม่ออก
- **แก้ไข:** เขียนใหม่ทั้งสองไฟล์ให้เลิกใช้ `dark:` classes ทั้งหมด เปลี่ยนไปใช้ระบบ component เดิมของแอป (`.btn`/`.input`/`.field`/`.label`/`.badge` และ CSS variable tokens จาก `globals.css`) กับ modal แบบ portal เหมือนหน้า `admin/clubs/page.tsx` และหน้าผลสแกนของเครื่องสแกน กันไม่ให้ modal ถูกซ้อนผิดตำแหน่งจาก ancestor ที่มี transform ด้วย
- **Human factors:** ปรับ checkbox ตั้งค่า ("1 คนรับได้ครั้งเดียว", "ต้องเช็คอินก่อน") ให้เป็นแถวที่กดได้ทั้งแถวแทนกล่องเล็ก ๆ และปุ่มในหน้าสแกนแจกรางวัลใหญ่ขึ้น เพราะหน้านี้ถูกใช้บนมือถือที่ถืออยู่ในระยะเอื้อมมือหน้าบูธ
- ตรวจสอบแล้วด้วย `npm run lint`, `npx tsc --noEmit`, `npm run build` ผ่านครบ พร้อมทดสอบจริงผ่าน dev-bypass session กับ local DB โดยเปิดหน้าสร้างรางวัล ตรวจ toggle ทั้งสองแถว และเดินผ่าน flow สแกน เห็นตัวอย่าง ยืนยัน ถ่ายรูป จนจบ ไม่พบร่องรอยของโหมดมืดหลงเหลือ

---

อัปเดตรอบนี้รวม PR #238 (แก้หน้าจอการรับรางวัลที่อ่านไม่ออกในโหมดมืด branch `fix/prizes-admin-ui-light-mode` merge commit `840c806`) ตัดแพ็กเกจเป็น **tag/release v2.10.1** สรุปวันที่ 19 ก.ย. 69

---

# ActiveCAMT v2.10.2 🎉

## ฝั่งแอดมิน/ทีมงาน (สิ่งที่ staff จะเห็น)

- **แก้กล้องสแกนแจกรางวัลที่กรอบดูเหมือนไม่อยู่ตรงกลาง** กล้องบนหน้าสแกนแจกรางวัลก่อนหน้านี้ไม่ได้ขอภาพจากกล้องเป็นสัดส่วนสี่เหลี่ยมจัตุรัส ทำให้กล้องบางเครื่อง (เช่นเว็บแคมโน้ตบุ๊ก) ได้ภาพทรงสูงเกินไปจนกรอบสแกนดูเหมือนไม่อยู่ตรงกลาง ตอนนี้ขอภาพเป็นสี่เหลี่ยมจัตุรัสแล้ว กรอบสแกนจึงอยู่ตรงตำแหน่งที่ควรอยู่
- **หน้าการรับรางวัลตอนนี้ดูเหมือนหน้าแอดมินอื่น ๆ แล้ว** ก่อนหน้านี้หน้านี้มีระยะห่างและหัวข้อของตัวเองแยกจากหน้าอื่น ทำให้ดูไม่กลมกลืนกับหน้าอื่นในระบบแอดมิน ตอนนี้ปรับให้ใช้สไตล์เดียวกับหน้าคำอุทธรณ์และหน้ารอตรวจสอบแล้ว
- **หน้าการรับรางวัลรองรับการเปลี่ยนภาษาแล้ว** ก่อนหน้านี้หน้านี้เป็นภาษาไทยล้วนไม่ว่าจะสลับภาษาระบบเป็นอะไรก็ตาม เป็นจุดเดียวในหน้าแอดมินที่ไม่รองรับการสลับภาษา ตอนนี้รองรับครบทั้ง 4 ภาษาแล้วเหมือนหน้าอื่น ๆ

## ฝั่งทีม (technical changelog)

- **Bug (กล้อง):** `PrizeAwardPanel.tsx` ไม่เคยส่ง `aspectRatio` ให้ `html5-qrcode` ตอนเปิดกล้อง ทำให้กล้องได้ความละเอียดตามธรรมชาติของอุปกรณ์ (ซึ่งมักเป็นแนวตั้งสูง) กรอบสแกนที่จริง ๆ อยู่กึ่งกลางของภาพอยู่แล้วจึงดูเหมือนไม่อยู่ตรงกลางเมื่อเทียบกับกรอบกล้องทั้งหมด แก้โดยขอ `aspectRatio: 1` จากกล้องและปรับกล่องกล้องให้เป็นสี่เหลี่ยมจัตุรัสตามไปด้วย
- **Layout:** `PrizesClient.tsx` มี wrapper ของตัวเองที่กำหนด max-width และ padding ซ้ำซ้อนกับ `AdminLayoutWrapper` ที่จัดการ padding ให้อยู่แล้ว ทำให้หน้าดูแคบและตำแหน่งหัวข้อไม่ตรงกับหน้าอื่น แก้โดยตัด wrapper ที่ซ้ำซ้อนออกและใช้โครงหัวข้อแบบเดียวกับ `admin/appeals` และ `admin/reviews`
- **i18n:** เพิ่ม key แปลภาษาใหม่ 62 รายการครบทั้ง EN/TH/MM/CN ใน `src/lib/i18n.ts` และเชื่อม `useLanguage()` เข้ากับทุก component ใน `PrizesClient.tsx` และ `PrizeAwardPanel.tsx` ที่แสดงข้อความซึ่งก่อนหน้านี้ยังไม่รองรับ
- ตรวจสอบแล้วด้วย `npm run lint`, `npx tsc --noEmit`, `npm run build` ผ่านครบ พร้อมทดสอบจริงผ่าน dev-bypass session กับ local DB อีกครั้งหลังแก้ไข

---

อัปเดตรอบนี้รวม PR #239 (แก้กรอบกล้องสแกน ปรับ layout ให้ตรงกับหน้าแอดมินอื่น และเพิ่มการรองรับหลายภาษาให้หน้าการรับรางวัล branch `fix/prizes-camera-layout-i18n` merge commit `521c0ec`) ตัดแพ็กเกจเป็น **tag/release v2.10.2** สรุปวันที่ 19 ก.ย. 69

---

# ActiveCAMT v2.10.3 🎉

## ฝั่งแอดมิน/ทีมงาน (สิ่งที่ staff จะเห็น)

- **กรอบสแกน QR ในหน้าแจกรางวัลอยู่ตรงกลางจริง ๆ แล้ว** รอบที่แล้วขอให้กล้องส่งภาพเป็นสี่เหลี่ยมจัตุรัสแล้ว แต่กล้องบางรุ่น (เช่นเว็บแคมโน้ตบุ๊กบางรุ่น) ไม่ยอมทำตามที่ขอจริง ๆ ยังส่งภาพเป็นสัดส่วนอื่นอยู่ ทำให้กรอบสแกนดูเหมือนค้างอยู่ด้านบนของกล่องกล้อง ตอนนี้แก้ให้ภาพจากกล้องอยู่ตรงกลางกล่องเสมอไม่ว่ากล้องจะส่งภาพสัดส่วนไหนมาก็ตาม
- **หน้าต่างแจกรางวัลอยู่กึ่งกลางจอแล้ว** ก่อนหน้านี้หน้าต่างนี้เด้งขึ้นมาจากด้านล่างจอแบบ bottom sheet ซึ่งไม่เหมือนกับหน้าต่างอื่น ๆ ในระบบแอดมิน (เช่นหน้าต่างสร้าง/แก้ไขชมรม) ตอนนี้ปรับให้อยู่กึ่งกลางจอเหมือนหน้าต่างอื่นแล้ว

## ฝั่งทีม (technical changelog)

- **Bug (กล้อง):** `aspectRatio: 1` ที่ส่งให้ `html5-qrcode` เป็นแค่คำขอ ไม่ใช่การบังคับ กล้องหลายรุ่นเพิกเฉยแล้วส่งภาพตามสัดส่วนธรรมชาติของตัวเองแทน (เช่น 4:3) ขณะที่ `html5-qrcode` กำหนดความกว้างของ `<video>` ตามกล่องคอนเทนเนอร์แต่ปล่อยความสูงเป็น auto โดยไม่จัดกึ่งกลางให้ พอภาพที่ได้ไม่เป็นสี่เหลี่ยมจัตุรัสจริง วิดีโอ (และกรอบสแกนที่ซ้อนอยู่บนวิดีโอ) จึงเตี้ยกว่ากล่องสี่เหลี่ยมและติดอยู่ด้านบนตามพฤติกรรม default ของ block element แก้โดยปรับ wrapper `#prize-qr-reader` ให้เป็น flex container แบบจัดกึ่งกลาง ทำให้วิดีโอกึ่งกลางกล่องเสมอไม่ว่าสัดส่วนจริงจากกล้องจะเป็นเท่าไหร่
- **UI:** overlay ของหน้าต่างแจกรางวัลใช้ `alignItems: "flex-end"` ทำให้เป็น bottom sheet มุมบนตัดตรง ต่างจากหน้าต่างอื่นในระบบแอดมินที่ใช้ `alignItems: "center"` ทั้งหมด (เช่นหน้าต่างของ `admin/clubs/page.tsx` และ overlay ในหน้าเครื่องสแกน) แก้ให้ตรงกับรูปแบบเดิมของระบบ
- ตรวจสอบแล้วด้วย `npm run lint`, `npm run build` ผ่านครบ (error เดียวที่เจอเป็นของเดิมในไฟล์ทดสอบที่ไม่เกี่ยวข้อง `battle-flow.test.ts`) และตรวจ DOM จริงผ่าน dev-bypass session โดยจำลององค์ประกอบขนาดเท่าที่ `html5-qrcode` สร้างจริง (กว้างคงที่ สูง auto ไม่เท่าคอนเทนเนอร์) เพื่อยืนยันว่าจัดกึ่งกลางได้จริงโดยไม่ต้องพึ่งกล้องจริง เพราะสภาพแวดล้อมทดสอบไม่มีกล้องให้เชื่อมต่อ

---

อัปเดตรอบนี้รวม PR #240 (แก้กรอบสแกน QR ให้อยู่กึ่งกลางกล่องกล้องจริง และปรับหน้าต่างแจกรางวัลให้อยู่กึ่งกลางจอเหมือนหน้าต่างอื่น branch `fix/prize-camera-vertical-center` merge commit `e7f6470`) ตัดแพ็กเกจเป็น **tag/release v2.10.3** สรุปวันที่ 19 ก.ย. 69

---

# ActiveCAMT v2.10.4 🎉

## ฝั่งนักศึกษา (สิ่งที่ user จะเห็น)

- **สถิติ "คะแนน" หน้าแดชบอร์ดตอนนี้ตรงกับคะแนนสะสมจริงแล้ว** ก่อนหน้านี้ตัวเลขในช่องคะแนนที่หน้าแดชบอร์ดอาจไม่ตรงกับคะแนนสะสมที่ได้จริงจากการเช็คอินกิจกรรม โดยเฉพาะกิจกรรมที่ให้คะแนนแยกจากคะแนนโบนัสประจำบ้าน ตอนนี้แก้ให้ตัวเลขนี้แสดงคะแนนสะสมส่วนตัวที่ได้รับจริงเท่านั้น

## ฝั่งแอดมิน/ทีมงาน (สิ่งที่ staff จะเห็น)

- **หน้าต่างยืนยัน "เช็คเอาท์" ที่เครื่องสแกน (กิจกรรมที่ต้องเช็คอิน 2 ครั้ง) แสดงข้อความและคะแนนถูกต้องแล้ว** ก่อนหน้านี้พอกดยืนยันเช็คเอาท์สำเร็จ หน้าต่างจะขึ้นข้อความ "เช็คอินสำเร็จ" ปนกับคะแนน 0 แม้ระบบจะให้คะแนนไปแล้วจริงในฐานข้อมูล ทำให้เจ้าหน้าที่หน้าบูธสับสนว่าได้คะแนนไปหรือยัง ตอนนี้แก้ให้ขึ้นข้อความ "เช็คเอาท์สำเร็จ" พร้อมคะแนนที่ได้รับจริง ครบทั้ง 4 ภาษา

## ฝั่งทีม (technical changelog)

### แก้บั๊กหน้าต่างยืนยันเช็คเอาท์และสถิติคะแนนหน้าแดชบอร์ด (PR #241, merge commit `f373274`)

- **Bug (scanner):** `ScannerService.confirmCheckout` เดิมคืนค่า status เป็น `"success"` เหมือนการเช็คอินทั่วไป จึงตกไปใช้ข้อความ generic "Check-in Successful!" ใน `STATUS_CONFIG` ที่หน้าสแกน และยังคืนค่า `points` จาก snapshot ของนักศึกษาที่ดึงมาก่อนเริ่ม transaction ให้คะแนน (จึงเป็น 0 เสมอ) แก้โดยให้คืนค่า status ใหม่ `"success_checkout"` พร้อมอ่านคะแนนล่าสุดกลับมาจากผลลัพธ์ของ `awardIndividualPoints` เอง (ไม่ใช่ snapshot เดิม) และเพิ่ม `STATUS_CONFIG.success_checkout` และคำแปล `scanCheckoutSuccess` ("เช็คเอาท์สำเร็จแล้ว!") ครบทั้ง 4 ภาษา รวมถึงเชื่อม `success_checkout` เข้ากับการล้าง cache ของลีดเดอร์บอร์ด (`revalidateLeaderboards()`) ที่เดิมมีแค่ `success`/`success_walk_in` เท่านั้น
- **Bug (dashboard):** สถิติ "คะแนน" ที่ `DashboardClient.tsx` เดิมรวมค่าจากฟิลด์ `event.pointsAwarded` ซึ่งจริง ๆ คือคะแนนโบนัสประจำบ้าน (house-winner bonus) ไม่ใช่คะแนนสะสมส่วนตัวของนักศึกษา (`individualPointsAwarded`) ทำให้ตัวเลขผิดสำหรับกิจกรรมส่วนใหญ่ ไม่ใช่แค่ปัญหา cache หรือเฉพาะกิจกรรมที่ต้องเช็คเอาท์อย่างที่คาดไว้แรก ๆ แก้โดยให้ `/api/events` (GET) คำนวณและส่งฟิลด์ใหม่ `pointsCredited` ต่อกิจกรรม (เช็คอินแล้ว และถ้าเป็นกิจกรรมที่ต้องเช็คเอาท์ต้องเช็คเอาท์เสร็จด้วย) แล้วให้ dashboard รวมคะแนนจาก `individualPointsAwarded` เฉพาะกิจกรรมที่ `pointsCredited` เป็นจริงเท่านั้น
- ตรวจสอบแล้วด้วย `npm test` (289 tests ผ่านหมด), `npm run lint`, `npx tsc --noEmit`, `npm run build` ผ่านครบ พร้อมทดสอบจริงกับ local DB ผ่าน dev-bypass session โดยยิง API ตรงด้วย QR token ที่เซ็นเองให้ตรงกับ scheme จริงของระบบ (ส่วนต่อขยาย Chrome ไม่ได้เชื่อมต่อรอบนี้) ยืนยันว่าเช็คเอาท์จริงคืนค่า `status: "success_checkout"` พร้อม `points: 10` (จากเดิม `"success"` กับ `0`) และ `/api/events` รายงาน `pointsCredited: true` หลังเช็คเอาท์ กับ `false` ก่อนเช็คเอาท์เสร็จถูกต้อง

---

อัปเดตรอบนี้รวม PR #241 (แก้บั๊กหน้าต่างยืนยันเช็คเอาท์ที่เครื่องสแกนและสถิติคะแนนหน้าแดชบอร์ด branch `fix/checkout-status-display` merge commit `f373274`) ตัดแพ็กเกจเป็น **tag/release v2.10.4** สรุปวันที่ 19 ก.ย. 69

---

# ActiveCAMT v2.10.5 🎉

## ฝั่งแอดมิน/ทีมงาน (สิ่งที่ staff จะเห็น)

- **กรอบสแกน QR ในหน้าแจกรางวัลใหญ่และชัดขึ้น เท่ากับหน้าสแกนเช็คอินหลักแล้ว** ก่อนหน้านี้กรอบสแกนที่หน้าแจกรางวัลเล็กกว่าและขอบบางกว่าหน้าสแกนเช็คอินหลัก ทำให้ดูไม่เหมือนเป็นระบบเดียวกัน ตอนนี้ปรับให้มีขนาดและเส้นขอบเท่ากันแล้ว
- **แก้ไขรางวัลที่สร้างไปแล้วได้แล้ว** ก่อนหน้านี้พอกดสร้างรางวัลไปแล้วจะแก้ไขอะไรไม่ได้เลย ต้องลบแล้วสร้างใหม่เท่านั้น ตอนนี้กดปุ่ม "แก้ไข" ที่การ์ดรางวัลแต่ละใบได้ทันที แก้ชื่อ กิจกรรมที่ผูก จำนวนที่ตั้งไว้ เงื่อนไขการแจก (คนละครั้งเดียว/ต้องเช็คอินก่อน) และเปิด/ปิดรับได้ครบ

## ฝั่งทีม (technical changelog)

- **UI (กล้อง):** `PrizeAwardPanel.tsx` ขอ `qrbox` จาก `html5-qrcode` แค่ 240x240 พร้อมกรอบกล่องกล้อง `border: 6px solid var(--bg-elevated)` ขณะที่หน้าสแกนหลัก (`admin/scanner/page.tsx`) ใช้ `qrbox` 280x280 กับกรอบ `8px solid var(--bg-surface)` และมี `boxShadow` ทำให้สองหน้าที่ควรเป็นภาษาภาพเดียวกันดูต่างกัน ปรับให้ตรงกันทั้งขนาดและสไตล์กรอบ
- **Feature (edit):** `PrizesClient.tsx` เดิมมีแค่ `CreatePrizeDialog` สำหรับสร้างอย่างเดียว ไม่มี dialog หรือปุ่มแก้ไขเลย ทั้งที่ backend (`PATCH /api/admin/prizes/[id]`) รองรับการแก้ไขทุกฟิลด์รวมถึง `status` อยู่แล้วตั้งแต่แรก ปรับ `CreatePrizeDialog` เป็น `PrizeFormDialog` ที่รับ prop `prize` แบบ optional ให้ทำงานได้ทั้งโหมดสร้าง (POST) และแก้ไข (PATCH) พร้อมเพิ่มช่องเลือกสถานะเปิด/ปิดรับเฉพาะตอนแก้ไข และเพิ่มปุ่ม "แก้ไข" ที่การ์ดรางวัลแต่ละใบสำหรับสิทธิ์ `canManage`
- **i18n:** เพิ่ม key ใหม่ 4 รายการ (`adminPrizesEditTitle`, `adminPrizesStatusField`, `adminPrizesStatusOptionOpen`, `adminPrizesStatusOptionClosed`) ครบทั้ง EN/TH/MM/CN ใน `src/lib/i18n.ts`
- ตรวจสอบแล้วด้วย `npm run lint`, `npx tsc --noEmit`, `npm run build` ผ่านครบ (error เดียวที่เจอเป็นของเดิมในไฟล์ทดสอบที่ไม่เกี่ยวข้อง `battle-flow.test.ts`)

---

อัปเดตรอบนี้รวม PR #242 (ปรับกรอบสแกน QR หน้าแจกรางวัลให้ตรงกับหน้าสแกนหลัก และเพิ่มการแก้ไขรางวัลที่สร้างไปแล้ว branch `fix/prizes-scanner-box-and-edit` merge commit `34b572e`) ตัดแพ็กเกจเป็น **tag/release v2.10.5** สรุปวันที่ 19 ก.ย. 69

---

# ActiveCAMT v2.10.6 🎉

## ฝั่งแอดมิน/ทีมงาน (สิ่งที่ staff จะเห็น)

- **แก้ปัญหาแนบรูปหลักฐานที่หน้าแจกรางวัลไม่ผ่านบนไอโฟน** ก่อนหน้านี้ถ้าถ่ายรูปด้วยกล้องไอโฟนตรง ๆ แล้วแนบที่หน้าแจกรางวัล มักจะขึ้น "อัปโหลดรูปไม่สำเร็จ" แบบกำกวมโดยไม่บอกสาเหตุ เพราะไอโฟนบันทึกรูปเป็นไฟล์แบบ HEIC ซึ่งระบบเดิมไม่รู้จัก ตอนนี้ถ้าเจอไฟล์แบบนี้จะขึ้นข้อความบอกวิธีแก้ชัดเจน (เปลี่ยนกล้องเป็นโหมด "เข้ากันได้มากที่สุด" หรือเลือกรูปที่เป็น JPG/PNG แทน) และหน้าจอจะแสดงเหตุผลจริงจากระบบแทนข้อความกลาง ๆ ด้วย
- **ปุ่มแนบรูปเลือกจากคลังภาพได้แล้ว ไม่ต้องถ่ายใหม่อย่างเดียว** ก่อนหน้านี้กดปุ่มแนบรูปแล้วจะเปิดกล้องถ่ายรูปทันทีเท่านั้น เลือกรูปที่มีอยู่แล้วในเครื่องไม่ได้เลย ตอนนี้เลือกได้ทั้งถ่ายใหม่และเลือกจากคลังภาพ
- **แนบรูปย้อนหลังให้รายการที่ "รอรูป" ได้แล้ว** ก่อนหน้านี้ถ้าลืมถ่ายรูปตอนแจกของ ระบบจะบอกให้ไปแนบทีหลังจากรายการ "รอรูป" แต่จริง ๆ แล้วไม่มีหน้าจอให้ทำแบบนั้นได้เลย ตอนนี้กดที่ตัวเลข "รอรูป" บนหน้ารายการรางวัลได้เลย จะเห็นรายชื่อคนที่ยังไม่มีรูปและแนบรูปให้แต่ละคนได้ทันที

## ฝั่งทีม (technical changelog)

- **Bug (อัปโหลดรูป):** `sniffImageType` ใน `src/lib/image-upload.ts` (ใช้ร่วมกันทั้งการอัปโหลดฟอร์มทั่วไปและรูปหลักฐานรางวัล) ตรวจ magic bytes ได้แค่ JPEG/PNG/GIF/WEBP ไม่รู้จักไฟล์ HEIC/HEIF ที่กล้องไอโฟนบันทึกเป็นค่าเริ่มต้น ทำให้ตกไปที่ error กลาง ๆ "File content is not a valid image." เสมอโดยไม่มีทางแก้ที่ชัดเจน เพิ่มฟังก์ชันตรวจ ISO base media container (`ftyp` box + major brand) เพื่อจับไฟล์ HEIC/HEIF โดยเฉพาะและคืน error message ที่บอกวิธีแก้ตรง ๆ
- **UI (client error):** `PrizeAwardPanel.tsx` เดิม throw `Error("upload failed")` แบบคงที่เมื่ออัปโหลดไม่สำเร็จ ไม่เคยอ่าน `error` จาก response body ของ `/api/forms/upload` เลย ทำให้ staff ไม่มีทางรู้สาเหตุจริงจากหน้าจอ แก้ให้ทั้ง `PrizeAwardPanel.tsx` และ dialog ใหม่ (ดูข้อถัดไป) อ่านและแสดงข้อความจาก server ตรง ๆ
- **UI (ช่องเลือกไฟล์):** ตัด attribute `capture="environment"` ออกจาก input เลือกรูปหลักฐาน เพราะ attribute นี้บังคับเปิดกล้องถ่ายตรงบนมือถือและซ่อนตัวเลือก "เลือกจากคลังภาพ" ไปเลย
- **Feature (รอรูป):** `PrizeService.listAwaitingPhoto` มีอยู่แล้วในโค้ดแต่ไม่เคยถูกเรียกใช้จากที่ไหนเลย ทั้ง route และ UI ไม่มี เพิ่ม `GET /api/admin/prizes/[id]/awaiting-photo` (gate ด้วย `canManage` เหมือน route พี่น้อง `GET /api/admin/prizes/[id]` ที่คืนข้อมูลชุดเดียวกัน และ scope ด้วย `canReachPrize` แบบเดียวกัน) กับ dialog ใหม่ใน `PrizesClient.tsx` ที่เปิดจากตัวเลข "รอรูป" เดิม ให้แนบรูปย้อนหลังผ่าน route แนบรูปเดิม (`POST /api/admin/prizes/claims/[claimId]/photo`) ที่มีอยู่แล้วและรองรับสิทธิ์ `canAward` อยู่แล้วโดยไม่ต้องแก้ไขเพิ่ม
- **ตรวจสอบสิทธิ์เข้าถึงแล้ว:** ให้ agent ตรวจ access-control/PDPA ของ route ใหม่แยกต่างหาก ยืนยันว่าการ gate ด้วย `canManage` สอดคล้องกับ route พี่น้องเดิม (`canManage` เป็นชุดย่อยของ `canAward` เสมอ จึงไม่มีทางเห็นรายการแล้วกดแนบรูปไม่ได้) การ scope ด้วย `canReachPrize` ถูกต้องตรงกับ route อื่น และข้อมูลที่คืนมามีแค่ชื่อ/รหัสนักศึกษา/เวลารับ ไม่มีข้อมูลการแพทย์
- ตรวจสอบแล้วด้วย `npm run lint`, `npx tsc --noEmit`, `npm run build` ผ่านครบ

---

อัปเดตรอบนี้รวม PR #243 (แก้ปัญหาอัปโหลดรูปหลักฐานรางวัลที่เป็นไฟล์ HEIC ให้เลือกรูปจากคลังภาพได้ และเพิ่มหน้าจอแนบรูปย้อนหลังให้รายการที่รอรูป branch `fix/prize-photo-heic-and-awaiting-list` merge commit `a4de1c0`) ตัดแพ็กเกจเป็น **tag/release v2.10.6** สรุปวันที่ 19 ก.ย. 69

---

# ActiveCAMT v2.10.7 🎉

## ฝั่งแอดมิน/ทีมงาน (สิ่งที่ staff จะเห็น)

- **ข้อความ "Internal Server Error" ตอนอัปโหลดไฟล์ไม่สำเร็จ เปลี่ยนเป็นบอกสาเหตุจริงแล้ว** หลังจากแก้ปัญหา HEIC ไปแล้ว ยังมีบางกรณีที่อัปโหลดรูปล้มเหลวแล้วขึ้นแค่ข้อความ "Internal Server Error" แบบกำกวม ไม่บอกอะไรเลยว่าเกิดอะไรขึ้น ตอนนี้ระบบจะแสดงเหตุผลจริงที่ทำให้บันทึกไฟล์ไม่สำเร็จแทน ช่วยให้ทีมงานวินิจฉัยและแก้ต้นเหตุ (เช่น ปัญหาการตั้งค่าที่เก็บไฟล์บนเซิร์ฟเวอร์) ได้จริง แทนที่จะเดาไม่ถูก

## ฝั่งทีม (technical changelog)

- **Bug (error handling):** `/api/forms/upload` (route ที่ใช้ร่วมกันทั้งการอัปโหลดฟอร์มทั่วไปและรูปหลักฐานรางวัล) เดิม catch ทุก error ที่ไม่ใช่ `ImageValidationError` แล้วคืนข้อความคงที่ "Internal Server Error" เสมอ ทิ้งเหตุผลจริงจาก `uploadFormFile` (`src/lib/form-file-storage.ts`) ไปเปล่า ๆ ไม่ว่าจะเป็น error จาก Supabase Storage หรือ error จากการเขียนไฟล์ลงดิสก์ของเซิร์ฟเวอร์เอง (ซึ่งเป็น path ที่ใช้งานจริงบน self-hosted deploy เพราะตั้งใจไม่ตั้งค่า `SUPABASE_*` ตามที่ระบุไว้ใน `docker-stack.yml`)
- **แก้ไข:** เพิ่มคลาส `StorageError` ใน `form-file-storage.ts` ที่พกข้อความที่ปลอดภัยและเจาะจง (ข้อความจาก Supabase driver หรือ error จริงจากระบบไฟล์อย่าง `EACCES`/`ENOSPC` สำหรับ path เขียนดิสก์) แล้วให้ route คืนข้อความนั้นแทนข้อความคงที่เดิม การแก้นี้ยังไม่ได้แก้ต้นเหตุที่แท้จริงของปัญหาอัปโหลด (น่าจะเป็นค่า `SUPABASE_*` ที่ยังค้างอยู่ใน env ของ Portainer stack หรือปัญหาสิทธิ์ไฟล์/volume แต่ไม่สามารถยืนยันได้โดยไม่มีสิทธิ์เข้าเซิร์ฟเวอร์จริง) แต่ทำให้การอัปโหลดที่ล้มเหลวครั้งถัดไปบอกสาเหตุจริงได้ทันที
- ตรวจสอบแล้วด้วย `npm run lint`, `npx tsc --noEmit`, `npm run build` ผ่านครบ

---

อัปเดตรอบนี้รวม PR #244 (แก้ให้การอัปโหลดไฟล์ที่ล้มเหลวแสดงเหตุผลจริงแทนข้อความ Internal Server Error แบบกำกวม branch `fix/surface-storage-upload-errors` merge commit `9575c0d`) ตัดแพ็กเกจเป็น **tag/release v2.10.7** สรุปวันที่ 19 ก.ย. 69

---

# ActiveCAMT v2.10.8 🎉

## ฝั่งทีม (technical changelog)

- **Bug (Docker/สิทธิ์ไฟล์):** จาก log จริงบนเซิร์ฟเวอร์พบ `EACCES: permission denied, mkdir '/app/.next/cache/fetch-cache'` ขึ้นซ้ำ ๆ ทุกครั้งที่มีคนเข้าเว็บ สาเหตุคือ `Dockerfile` คัดลอกโฟลเดอร์ `.next` เข้า image ด้วยสิทธิ์ root ก่อนที่จะสลับมาใช้ผู้ใช้ `nextjs` ทำให้แอปที่รันด้วยผู้ใช้ `nextjs` เขียนไฟล์ cache ภายในของ Next.js เองไม่ได้เลย (ไม่ทำให้หน้าเว็บพังเพราะ Next.js แค่ log แล้วข้ามไป แต่ทำให้ fetch cache ไม่ถูกบันทึกจริง) แก้โดยเพิ่ม `--chown=nextjs:nodejs` ให้กับคำสั่ง `COPY .next` เหมือนที่ `public/uploads` และ `.uploads-private` ทำอยู่แล้ว
- **วินิจฉัยเพิ่มเติม (ไม่ใช่การแก้ต้นเหตุ):** log เดียวกันเผยสาเหตุจริงที่ทำให้แนบรูปหลักฐานรางวัลไม่สำเร็จคือ `TypeError: Failed to parse body as FormData.` ซึ่งเกิดขึ้นก่อนโค้ดของแอปจะได้ทำงานด้วยซ้ำ (`uploadFormFile` ไม่เคยถูกเรียกเลย) ตรวจแล้วว่าไม่ใช่ middleware ของแอป (`src/proxy.ts` ยกเว้น path `/api/*` ทั้งหมดออกจาก matcher อยู่แล้ว) จึงน่าจะเป็นปัญหาที่ชั้น Nginx reverse proxy ของมหาวิทยาลัยที่อยู่หน้าคอนเทนเนอร์ (ตามที่ระบุใน `docs/wiki/development/deployment-architecture.md`) ซึ่งอยู่นอกขอบเขตของโค้ดใน repo นี้ เพิ่ม log ที่บันทึก `Content-Type`/`Content-Length` จริงตอนเกิดปัญหานี้ และเปลี่ยนข้อความที่ staff เห็นจาก "Internal Server Error" เป็นข้อความเฉพาะเจาะจงขึ้น เพื่อช่วยตามหาสาเหตุที่ชั้น reverse proxy ต่อไป
- ตรวจสอบแล้วด้วย `npm run lint`, `npx tsc --noEmit`, `npm run build` ผ่านครบ

---

อัปเดตรอบนี้รวม PR #245 (แก้สิทธิ์ไฟล์ .next ใน Docker image และเพิ่ม log วินิจฉัยปัญหาอัปโหลดไฟล์ที่ชั้น reverse proxy branch `fix/docker-next-cache-perms-and-formdata-diagnostics` merge commit `518ed7a`) ตัดแพ็กเกจเป็น **tag/release v2.10.8** สรุปวันที่ 19 ก.ย. 69

---

# ActiveCAMT v2.10.9 🎉

## ฝั่งแอดมิน/ทีมงาน (สิ่งที่ staff จะเห็น)

- **ถ่ายรูปหลักฐานรางวัลจากไอโฟนแล้วแนบได้จริงแล้ว** หลังจากไล่ตามปัญหาแนบรูปไม่สำเร็จมาหลายรอบ ในที่สุดก็เจอสาเหตุจริง เบราว์เซอร์ Safari บนไอโฟนมีบั๊กที่ทำให้บางครั้งส่งรูปที่ถ่ายไปไม่ครบ ทั้งที่หน้าจอดูเหมือนกำลังอัปโหลดอยู่ปกติ ทำให้ระบบได้รับคำขอที่ไม่มีเนื้อหารูปแนบมาด้วยเลย ตอนนี้เปลี่ยนวิธีอัปโหลดรูปที่หน้าแจกรางวัลและหน้าแนบรูปย้อนหลังให้ไม่ใช้วิธีที่มีบั๊กนี้อีกต่อไป

## ฝั่งทีม (technical changelog)

- **Bug (iOS Safari):** log จริงจากเซิร์ฟเวอร์ (จากการเพิ่ม log วินิจฉัยใน PR ก่อนหน้า) เผยให้เห็นว่าคำขออัปโหลดที่ล้มเหลวมี `Content-Type` เป็น `multipart/form-data; boundary=----WebKitFormBoundary...` ที่ถูกต้อง แต่ `Content-Length` เป็น `0` แปลว่าส่วนหัวคำขอถูกส่งไปถูกต้อง แต่เนื้อหาไฟล์เองไม่ถูกแนบไปด้วยเลย ตรงกับบั๊กที่รู้จักกันดีของ WebKit ที่ `fetch()` บางครั้งไม่แนบเนื้อหาของ `FormData` ที่มี `File`/`Blob` อยู่ข้างใน โดยเฉพาะช่วงที่เพิ่งกลับมาจากแอปกล้องของระบบ ซึ่งตรงกับ flow "ถ่ายรูป" ของหน้าแจกรางวัลพอดี
- **แก้ไข:** เพิ่ม `src/lib/xhr-upload.ts` (`uploadFormViaXHR`) ที่อัปโหลดผ่าน `XMLHttpRequest` แทน `fetch()` เพราะการเข้ารหัส multipart ของ XHR ไม่มีบั๊กนี้ และเป็นวิธีแก้มาตรฐานที่ใช้กันทั่วไปสำหรับบั๊กนี้โดยเฉพาะ เปลี่ยนจุดอัปโหลดรูปทั้งสองจุดของฟีเจอร์รางวัล (ขั้นตอนถ่ายรูปหลังยืนยันแจกรางวัลใน `PrizeAwardPanel.tsx` และหน้าต่างแนบรูปย้อนหลังใน `PrizesClient.tsx`) ให้ใช้ฟังก์ชันนี้แทน
- ตรวจสอบแล้วด้วย `npm run lint`, `npx tsc --noEmit`, `npm run build` ผ่านครบ

---

อัปเดตรอบนี้รวม PR #246 (แก้บั๊ก fetch() ของ iOS Safari ที่ทำให้แนบรูปหลักฐานรางวัลจากไอโฟนไม่สำเร็จ branch `fix/ios-safari-fetch-formdata-photo-upload` merge commit `f529546`) ตัดแพ็กเกจเป็น **tag/release v2.10.9** สรุปวันที่ 19 ก.ย. 69

---

# ActiveCAMT v2.10.10 🎉

## ฝั่งแอดมิน/ทีมงาน (สิ่งที่ staff จะเห็น)

- **ไล่ตามปัญหาแนบรูปหลักฐานรางวัลจากไอโฟนต่อ (รอบที่ 3)** การแก้ใน v2.10.9 (เปลี่ยนจาก fetch เป็น XMLHttpRequest) ยังไม่แก้ปัญหาจริง เพราะทดสอบซ้ำหลังปิดแอปแล้วเปิดใหม่ทั้งหมดก็ยังเจอ error เดิมเป๊ะ ๆ รอบนี้เปลี่ยนวิธีสร้างข้อมูลรูปก่อนอัปโหลดแทน (สงสัยว่าการห่อข้อมูลเป็น File object ที่สร้างขึ้นเองอาจเป็นสาเหตุ) พร้อมเพิ่มข้อความแจ้งเตือนที่เห็นได้ทันทีบนหน้าจอ ถ้าข้อมูลรูปว่างเปล่าตั้งแต่ก่อนอัปโหลด เพื่อช่วยยืนยันสาเหตุที่แท้จริงในการทดสอบรอบถัดไป ยังไม่ยืนยันว่าแก้หายขาด ต้องรอผลทดสอบจริงอีกครั้ง

## ฝั่งทีม (technical changelog)

- **Bug (ต่อเนื่องจาก v2.10.9):** การแก้ครั้งก่อนเปลี่ยนจาก `fetch()` เป็น `XMLHttpRequest` โดยตั้งสมมติฐานว่าเป็นบั๊กเฉพาะของ `fetch()` แต่ทดสอบซ้ำหลัง force-quit และเปิด PWA ใหม่ทั้งหมด (ตัดความเป็นไปได้เรื่อง JS เก่าค้าง cache) ยังเจอ error เดิมเป๊ะ ๆ คือ `Content-Type` เป็น multipart boundary ที่ถูกต้อง แต่ `Content-Length` เป็น `0` เมื่อทั้ง `fetch()` และ `XHR` ล้มเหลวเหมือนกันทุกประการ แปลว่าปัญหาไม่ได้อยู่ที่ transport layer แต่อยู่ก่อนหน้านั้น คือตัวข้อมูลที่ส่งให้ `FormData` เอง
- **สมมติฐานใหม่:** `compressImageFile()` คืนค่ารูปที่บีบอัดแล้วโดยห่อด้วย `new File([blob], ...)` ซึ่ง WebKit มีบั๊กที่รู้จักกันเกี่ยวกับการแปลง `FormData` entry ที่เป็น `File` object ที่สร้างขึ้นเอง (ต่างจาก `Blob` ธรรมดา หรือ `File` ที่มาจาก `<input>` โดยตรง) แก้โดยแปลง `File` กลับเป็น `Blob` ธรรมดาด้วย `.slice()` ก่อน append เข้า `FormData` พร้อมระบุชื่อไฟล์แยกเป็น argument ที่ 3 แทนที่จะพึ่ง `.name` ของ `File`
- **เพิ่มการตรวจสอบที่มองเห็นได้จากหน้าจอ:** เพิ่มการเช็ค `compressed.size === 0` ก่อนเริ่มอัปโหลดเลย ถ้าข้อมูลว่างเปล่าจะขึ้นข้อความเฉพาะเจาะจงทันทีบนหน้าจอ staff โดยไม่ต้องเปิด log บนเซิร์ฟเวอร์ดู ช่วยแยกให้ชัดว่าปัญหาเกิดตอนสร้างข้อมูลรูป (ฝั่ง client) หรือเกิดหลังจากนั้น (ฝั่งเครือข่าย/เซิร์ฟเวอร์)
- ตรวจสอบแล้วด้วย `npm run lint`, `npx tsc --noEmit`, `npm run build` ผ่านครบ (ยังไม่มีอุปกรณ์ iOS จริงให้ทดสอบตรง ต้องรอผลทดสอบจริงจากทีมงาน)

---

อัปเดตรอบนี้รวม PR #247 (ไล่ตามปัญหาแนบรูปหลักฐานรางวัลจากไอโฟนต่อ เปลี่ยนวิธีสร้างข้อมูลรูปก่อนอัปโหลดและเพิ่มการแจ้งเตือนที่มองเห็นได้จากหน้าจอ branch `fix/prize-photo-blob-formdata-serialization` merge commit `b5feea5`) ตัดแพ็กเกจเป็น **tag/release v2.10.10** สรุปวันที่ 19 ก.ย. 69

---

# ActiveCAMT v2.10.11 🎉

## ฝั่งนักศึกษา (สิ่งที่ user จะเห็น)

- **แนบไฟล์คำถามในแบบฟอร์ม (เช่นรูปหลักฐาน) จากไอโฟนได้เสถียรขึ้น** บั๊กเดียวกับที่เคยทำให้แนบรูปหลักฐานรางวัลจากไอโฟนไม่สำเร็จ ก็ทำให้การแนบไฟล์ในแบบฟอร์มทั่วไป (เช่นแบบทดสอบก่อน/หลังกิจกรรม) มีโอกาสล้มเหลวแบบเดียวกันด้วย ตอนนี้แก้ให้แล้ว

## ฝั่งแอดมิน/ทีมงาน (สิ่งที่ staff จะเห็น)

- **แนบรูปหลักฐานเช็กอินแบบ evidence และรูปหลักฐานตอนเช็กเอาต์ที่หน้าสแกนจากไอโฟนได้เสถียรขึ้น** เช่นเดียวกับรูปหลักฐานรางวัล จุดแนบรูปอีกสองจุดนี้ก็ใช้วิธีอัปโหลดแบบเดียวกับที่เคยมีบั๊กบนไอโฟน ตอนนี้แก้ให้ใช้วิธีเดียวกับที่แก้ให้หน้าแจกรางวัลไปแล้วครบทั้งสามจุด

## ฝั่งทีม (technical changelog)

- **Bug (iOS Safari, จุดที่เหลือ):** log จริงที่ยกมาในหัวข้อ "Form file upload: failed to parse multipart body" ยืนยันว่า `/api/forms/upload` (route ที่ใช้ร่วมกันทั้งฟอร์ม, evidence check-in, เช็คเอาต์ที่เครื่องสแกน และรูปหลักฐานรางวัล) ยังเจอบั๊ก WebKit เดิม (`Content-Type` เป็น multipart boundary ที่ถูกต้อง แต่ `Content-Length` เป็น `0`) เพราะการแก้ใน v2.10.9-v2.10.10 ทำเฉพาะสองจุดของฟีเจอร์รางวัล (`PrizeAwardPanel.tsx`, `PrizesClient.tsx`) เท่านั้น ยังเหลืออีกสามจุดที่เรียก endpoint เดียวกันด้วย `fetch()` ตรง ๆ และห่อรูปด้วย `new File(...)` ที่ `compressImageFile()` คืนมา ได้แก่การแนบไฟล์คำถามในแบบฟอร์ม (`dashboard/history/page.tsx`), รูปหลักฐาน evidence check-in (`dashboard/events/[id]/evidence/page.tsx`) และรูปหลักฐานเช็คเอาต์ที่เครื่องสแกน (`admin/scanner/page.tsx`)
- **แก้ไข:** ใช้ `uploadFormViaXHR` (`src/lib/xhr-upload.ts`) เดียวกับที่แก้ให้ฟีเจอร์รางวัลไปแล้วกับทั้งสามจุดที่เหลือ พร้อมแปลง `File` กลับเป็น `Blob` ธรรมดาด้วย `.slice()` ก่อน append เข้า `FormData` และเพิ่มการเช็ค `size === 0` ก่อนอัปโหลดเหมือนกันทุกจุด ทำให้ตอนนี้ทุกจุดที่โพสต์ไฟล์/รูปเข้า `/api/forms/upload` ใช้วิธีอัปโหลดเดียวกันครบแล้ว ไม่มีจุดไหนเหลือใช้ `fetch()` แบบเดิมอีก
- ตรวจสอบแล้วด้วย `npm run lint`, `npm run build` ผ่านครบ (error เดียวที่เจอเป็นของเดิมในไฟล์ทดสอบที่ไม่เกี่ยวข้อง `battle-flow.test.ts`)

---

อัปเดตรอบนี้รวม PR #248 (แก้บั๊ก fetch() ของ iOS Safari ที่จุดอัปโหลดไฟล์/รูปที่เหลืออีกสามจุด ได้แก่แบบฟอร์ม evidence check-in และเช็คเอาต์ที่เครื่องสแกน branch `fix/ios-safari-formdata-remaining-uploads` merge commit `ac27958`) ตัดแพ็กเกจเป็น **tag/release v2.10.11** สรุปวันที่ 19 ก.ย. 69
