# ActiveCAMT v2.8.0 🎉

# อัปเดต ActiveCAMT ประจำวันที่ 17 ก.ย. 69

สรุปสำหรับวันที่ 17 ก.ย. 69 เอาไว้ลง Discord และให้ทีมที่เกี่ยวข้องอ่านกัน

---

## ฝั่งแอดมิน/ทีมงาน (สิ่งที่ staff จะเห็น)

- **แก้ checkbox "Require check-out" ในหน้าแก้ไขกิจกรรม (`/admin/events`) ให้กดติดง่ายขึ้น** เดิมต้องกดตรงกล่องเล็ก ๆ เป๊ะ ๆ เท่านั้นถึงจะติ๊กได้ ตอนนี้กดตรงไหนของแถวก็ได้ (ไอคอน ข้อความ หรือกล่อง) แล้วติ๊กถูกครั้งเดียวแน่นอน พร้อมเปลี่ยนหน้าตากล่องติ๊กให้เป็นสไตล์ของแอปเอง แทนกล่องติ๊กมาตรฐานของเบราว์เซอร์
- **แก้ไขรายละเอียดคำสั่งซื้อในร้านค้าได้แล้วหลังลูกค้าสั่งซื้อไปแล้ว** ถ้าลูกค้าลืมเลือกไซส์/ตัวเลือก กรอกชื่อ-เบอร์บนเสื้อผิด หรือที่อยู่จัดส่งพิมพ์ผิด แอดมินหรือเจ้าของร้าน (ประธานชมรม/สาขา/ผู้ขาย) กดปุ่ม "แก้ไขรายละเอียด" ในหน้ารายการคำสั่งซื้อ (`/admin/shop`) เพื่อแก้ให้ถูกต้องได้เลย โดยไม่ต้องให้ลูกค้าสั่งใหม่ ใช้ได้กับคำสั่งซื้อทุกสถานะ ทั้งที่รออนุมัติ อนุมัติแล้ว หรือถูกปฏิเสธไปแล้ว
- **เปลี่ยนชื่อสินค้าหรือชื่อตัวเลือกในร้านค้าแล้ว ออร์เดอร์เก่าจะอัปเดตตามให้อัตโนมัติ** ไม่ต้องกังวลว่ารายการคำสั่งซื้อในหน้าแอดมินจะแยกเป็นชื่อเก่า/ชื่อใหม่คนละกลุ่มอีกต่อไปหลังเปลี่ยนชื่อสินค้า และมีสคริปต์ให้รันครั้งเดียวเพื่อแก้ย้อนหลังให้ออร์เดอร์ที่เปลี่ยนชื่อไปแล้วก่อนฟีเจอร์นี้จะมีอยู่ด้วย
- **หน้าต่าง "แก้ไขรายละเอียด" ของคำสั่งซื้อในร้านค้า แก้จำนวนสั่งซื้อได้แล้ว** นอกจากไซส์/ตัวเลือกที่แก้ได้อยู่แล้ว ตอนนี้แก้ "จำนวน" ของรายการนั้นได้ด้วย ถ้าแก้แค่จำนวนราคาต่อชิ้นจะไม่เปลี่ยน (คิดแค่ยอดรวมใหม่) แต่ถ้าเปลี่ยนตัวเลือกด้วยราคาจะคำนวณใหม่ตามตัวเลือกที่เลือก พร้อมขยายหน้าต่างให้กว้างขึ้นเพราะเดิมดูอึดอัดเกินไป

---

## ฝั่งทีม (technical changelog)

### แก้ปุ่มติ๊ก require-checkout กดพลาดง่าย (PR #230, merge commit `395a853`)

- **Bug:** `<label>` ของ checkbox มีทั้ง `onClick` ที่สั่ง toggle state เอง และการส่งต่อคลิกแบบ native ของ `<label>` ที่ครอบ `<input type="checkbox">` อยู่แล้ว ทำให้สองกลไกนี้ชนกันเองจนกดที่ข้อความ/ไอคอนไม่ค่อยติด ต้องกดตรงกล่องเล็ก ๆ เป๊ะ ๆ เท่านั้น
- **แก้ไข:** เอา `onClick` ที่ซ้ำซ้อนออก ให้เหลือแค่ `onChange` บน input เหมือน pattern ที่ใช้อยู่แล้วในไฟล์เดียวกัน (เช่น checkbox กรองนักศึกษาไทย/นานาชาติ) พร้อมเปลี่ยนกล่องติ๊กเป็นสไตล์ custom (กรอบ/พื้นสีธีม + ไอคอนถูกจาก Lucide) โดย input จริงยังอยู่ครอบเต็มกล่องแบบไม่แสดงผล เพื่อคง accessibility/form semantics เดิม
- ไม่มีการเปลี่ยน schema/DB เป็น UI fix ล้วน ตรวจสอบแล้วด้วย `npm run build` และ `npx tsc --noEmit` ผ่านครบ ไม่มี error ใหม่

### ให้แอดมิน/เจ้าของร้านแก้ไขคำสั่งซื้อได้ พร้อม sync ชื่อสินค้า/ตัวเลือกที่เปลี่ยนไปยังออร์เดอร์เก่า (PR #231, merge commit `f1a2314`)

- **Feature:** เพิ่ม `PUT /api/admin/shop/orders/[id]` ให้แอดมินร้าน (หรือประธานชมรม/สาขา/ผู้ขายที่ scoped) แก้ไขตัวเลือก (variant), ข้อความ "ระบุเอง" (Other specify), คำตอบช่องกรอกเอง (custom fields), หมายเหตุ, และข้อมูลผู้รับ/ที่อยู่จัดส่ง (เฉพาะออร์เดอร์แบบจัดส่ง) ของคำสั่งซื้อที่มีอยู่แล้วได้ทุกสถานะ ทั้ง pending/approved/rejected แก้ปัญหาที่ผู้ซื้อลืมเลือกตัวเลือกหรือกรอกไม่ครบตอนสั่งซื้อ
- **สิทธิ์เข้าถึง:** ใช้ gate เดียวกับปุ่มอนุมัติ/ปฏิเสธเดิมทุกประการ คือ `resolveShopAccess` ร่วมกับ `classifyOrdersByScope` แบบ fullyOwned ประธานที่ scoped แก้ไขได้เฉพาะออร์เดอร์ที่ทุกรายการเป็นของชมรม/สาขาตัวเองเท่านั้น และทุกการแก้ไขถูกบันทึกลง audit log แบบ append-only เหมือน action อื่นในหน้านี้ ตรวจสอบเพิ่มเติมด้วย pdpa-access-guard subagent แล้วไม่พบช่องโหว่เรื่องสิทธิ์เข้าถึงหรือ PII
- **แก้บั๊ก re-price ที่พบระหว่างพัฒนา:** ถ้าไม่ระวัง การแก้ไขจะคำนวณราคาของทุกรายการใหม่ตามราคาสินค้าปัจจุบันเสมอ แม้รายการนั้นไม่ได้ถูกแตะเลย ตอนนี้จะคำนวณราคาใหม่เฉพาะรายการที่ตัวเลือก/คำตอบเปลี่ยนจริงเท่านั้น ราคาของรายการที่ไม่ได้แก้ไขจะคงเดิมตามตอนซื้อ พร้อม re-validate stock ภายใต้ row lock เดียวกับ flow revert เดิม เพื่อกันการ race กับการแก้ไข/สั่งซื้อพร้อมกัน
- **แก้ sync ชื่อสินค้า/ตัวเลือก:** เดิมเปลี่ยนชื่อสินค้าหรือชื่อตัวเลือก (variant label) แล้วออร์เดอร์เก่าที่สั่งไปแล้วจะยังโชว์ชื่อเดิมตลอดไป ทำให้ filter/รายงานแยกเป็นสองชื่อ ตอนนี้เปลี่ยนชื่อสินค้า/ตัวเลือกจะ sync ไปยังทุกออร์เดอร์เก่าที่ใช้ชื่อนั้น รวมออร์เดอร์ที่ approved/rejected ไปแล้วด้วย โดยจับคู่ prefix แบบ exact string (ไม่ใช้ SQL `LIKE` wildcard) เพื่อกันเคสชื่อที่มีอักขระ `%`/`_` หลุดไปแมตช์ผิดแถว
- ไม่มีการเปลี่ยน schema/DB ตรวจสอบแล้วด้วย `npm run lint`, `npx tsc --noEmit`, `npm run build`, และ `npm test` (216 tests) ผ่านครบ ไม่มี error ใหม่

### สคริปต์แก้ข้อมูลย้อนหลังสำหรับ productName/variantLabel ที่ค้างมาจากการเปลี่ยนชื่อก่อนหน้านี้ (PR #232, merge commit `e11ab71`; แก้บั๊กใน PR #233, merge commit `101761f`)

- **Feature:** ฟีเจอร์ sync การเปลี่ยนชื่อ (PR #231) ทำงานแค่กับการเปลี่ยนชื่อ "รอบต่อไป" เท่านั้น ไม่ได้ย้อนแก้ออร์เดอร์ที่เปลี่ยนชื่อไปแล้วก่อนฟีเจอร์นี้จะมีอยู่ จึงเพิ่ม `scripts/backfill-shop-order-item-names.mjs` เป็นสคริปต์ one-off ที่ sync `product_name`/`variant_label` ของทุกออร์เดอร์เก่าให้ตรงกับชื่อสินค้า/ตัวเลือกปัจจุบัน (รันด้วยมือครั้งเดียวจาก Portainer console ไม่ใช่ส่วนหนึ่งของ CI) มี `--dry-run` ให้ดูก่อนว่าจะเปลี่ยนอะไรบ้าง และรันซ้ำได้อย่างปลอดภัย (idempotent)
- **Bug ที่เจอตอนรัน dry-run จริงบน prod:** เวอร์ชันแรกลองแยก label "แบบระบุเอง" (Other specify) ด้วยการหาตำแหน่ง `": "` ตัวแรกในข้อความ ซึ่งพังกับตัวเลือกที่ label ยาวและมี `": "` อยู่ในตัวเองอยู่แล้ว (เช่น "...Enter: [Size | ...]") ทำให้ตัดผิดตำแหน่งจนข้อความซ้ำกันเอง ทั้งที่ค่าที่บันทึกไว้ถูกต้องอยู่แล้ว ไม่ต้องแก้อะไรเลย
- **แก้ไข:** เปลี่ยนมาเช็คแบบ exact prefix กับ label ปัจจุบัน (`left()`/`char_length()` ไม่ใช้ `LIKE`) รายการที่ยืนยันได้ว่าตรงกับ label ปัจจุบันอยู่แล้วจะไม่ถูกแตะเลย ส่วนรายการที่ label ของตัวเลือกเองเคยถูกเปลี่ยนชื่อมาก่อน (ไม่ใช่แค่ชื่อสินค้า) จะถูกแยกไปรายงานว่า "ต้องตรวจสอบเอง" แทนการเดา ทดสอบซ้ำกับ local DB ทั้งสองเคสแล้วถูกต้อง

### ให้แก้ "จำนวน" ในหน้าต่างแก้ไขคำสั่งซื้อได้ด้วย พร้อมขยายหน้าต่างให้กว้างขึ้น (PR #234, merge commit `29c2389`)

- **Feature:** เพิ่มช่อง "จำนวน" ต่อรายการในหน้าต่าง "แก้ไขรายละเอียด" เดิม (`PUT /api/admin/shop/orders/[id]`) แก้แค่จำนวนจะไม่ re-price (คง unitPrice เดิมตามตอนซื้อ คิดแค่ยอดรวมใหม่) แต่ถ้าเปลี่ยนตัวเลือกด้วยจะ re-price ตามราคาปัจจุบันเหมือนเดิม ทั้งสองแบบ re-validate stock ให้ตาม `(variant, quantity)` ใหม่ภายใต้ row lock เดียวกับ flow revert
- **ค่าส่ง:** ออร์เดอร์แบบจัดส่งที่สินค้ามีค่าส่งแบบขั้นตามจำนวน (`deliveryTiers`) จะคำนวณค่าส่งใหม่ให้ด้วย แต่คำนวณใหม่เฉพาะตอนที่จำนวนเปลี่ยนจริงเท่านั้น เพื่อไม่ให้การแก้ไขอื่นที่ไม่เกี่ยวไปขยับค่าส่งที่บันทึกไว้ตั้งแต่ตอนซื้อ
- ไม่ re-check `maxPerOrder` (limit ต่อคนฝั่งผู้ซื้อ) เพราะเป็นการแก้ไขของแอดมินโดยตรง ไม่ใช่การซื้อของผู้ซื้อเอง
- ขยายหน้าต่างแก้ไขจาก 520px เป็น 640px (เท่ากับฟอร์มแก้ไขสินค้า) ตามฟีดแบ็กว่าเดิมแคบไป
- ตรวจสอบแล้วด้วย `npm run lint`, `npx tsc --noEmit`, `npm run build`, `npm test` (216 tests) ผ่านครบ พร้อมทดสอบจริงกับ local DB ผ่าน dev-bypass session (ยิง API ตรง ไม่ mock): แก้แค่จำนวนคำนวณยอดถูกต้องโดยไม่ re-price, ลองสั่งเกิน stock ถูกปฏิเสธด้วย error ชัดเจนและไม่มีอะไรถูกบันทึก, สลับตัวเลือกจนข้ามขั้นค่าส่งคำนวณค่าส่งใหม่ถูกต้อง, แก้แค่หมายเหตุไม่กระทบราคา/จำนวน/ค่าส่งของรายการอื่นเลย

---

อัปเดตรอบนี้รวม PR #230 (แก้ checkbox require-checkout กดพลาดง่าย branch `fix/checkout-checkbox-click-area` merge commit `395a853`), PR #231 (แก้ไขคำสั่งซื้อในร้านค้าได้หลังสั่งซื้อ พร้อม sync ชื่อสินค้า/ตัวเลือกไปยังออร์เดอร์เก่า branch `fix/shop-order-edit-and-rename-sync` merge commit `f1a2314`), PR #232 และ #233 (สคริปต์แก้ข้อมูลย้อนหลังสำหรับชื่อสินค้า/ตัวเลือกเก่า branch `chore/backfill-shop-order-item-names` และ `fix/backfill-script-colon-split-bug` merge commit `e11ab71` และ `101761f`) และ PR #234 (แก้จำนวนในหน้าต่างแก้ไขคำสั่งซื้อได้ พร้อมขยายหน้าต่าง branch `feat/shop-order-edit-quantity` merge commit `29c2389`) ตัดแพ็กเกจเป็น **tag/release v2.8.0** สรุปวันที่ 17 ก.ย. 69
